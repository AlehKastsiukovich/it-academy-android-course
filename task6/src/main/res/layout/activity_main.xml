<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="10dp"
    tools:context=".activity.MainActivity">

    <TextView
        android:id="@+id/internalStorageTextView"
        style="@style/StorageTypeStyle"
        android:text="@string/internal_storage"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@+id/externalStorageTextView"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/externalStorageTextView"
        style="@style/StorageTypeStyle"
        android:text="@string/external_storage"
        app:layout_constraintLeft_toRightOf="@+id/internalStorageTextView"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:orientation="horizontal"
        app:layout_constraintTop_toBottomOf="@+id/internalStorageTextView">

        <ListView
            android:id="@+id/filesListView"
            style="@style/FileListStyle" />

        <ListView
            android:id="@+id/externalStorageFilesListView"
            style="@style/FileListStyle" />
    </LinearLayout>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/addFileButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:backgroundTint="@color/black"
        android:contentDescription="@string/add_new_file"
        android:src="@drawable/ic_baseline_add_24"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:tint="@color/white" />

</androidx.constraintlayout.widget.ConstraintLayout>